/**
 * File nằm trên đĩa và được mọi worker đọc chung.
 * `.gitignore` đã loại `playwright/.auth/`, vì file chứa cookie đăng nhập.
 */
export const HYBRID_AUTH_FILE = "playwright/.auth/admin-hybrid.json";
